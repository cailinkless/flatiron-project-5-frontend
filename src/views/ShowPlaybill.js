import React from 'react'

// Make sure to throw error if id number is not in database

const ShowPlaybill = () => 

    <div>
        <h1>I'm the Show Playbill View!</h1>
    </div>

export default ShowPlaybill;